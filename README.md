# Note Transcribe

Transcribe audio with Whisper and use Ollama for LLM tasks.

## Requirements
- Docker & Docker Compose
- NVIDIA GPU + nvidia-docker (for GPU support)

## Quick Start
```bash
git clone https://github.com/yourusername/note-transcribe.git
cd note-transcribe
docker compose up -d
```